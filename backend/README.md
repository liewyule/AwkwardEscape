# Backend (Placeholder)

This project is intentionally backend-light. The app uses Supabase for auth and storage, so there is no custom server.

If you want a custom backend later, this folder is where to put it (API routes, webhooks, or admin scripts).
